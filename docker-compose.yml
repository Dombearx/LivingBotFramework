services:
  livingbot:
    env_file:
      - ./.env
    build:
      context: ./
      dockerfile: ./Dockerfile
    restart: unless-stopped
    ports:
      - "9080:9080"
    volumes:
      - ./data:/app/data
